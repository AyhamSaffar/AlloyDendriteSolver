[project]
name = "data"
version = "0.1.0"
description = "Environment for running analysis on script outputs."
requires-python = ">=3.14"
dependencies = [
    "ipykernel<7.0.0",
    "matplotlib>=3.10.8",
    "pandas>=3.0.1",
]
